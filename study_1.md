# 마크다운으로 저장
git으로 항상 올려보기

실습

git init

git remote add origin https://github.com/kasangyong/TIL.git --> 주소

git add file_name

git commit -m "~~"

git status

git log --oneline


git push origin master

git pull origin master

git clone https://github.com/kasangyong/TIL.git --> 주소

git remove -v 

.gitignore에 파일명 입력하면 git이 인식 못함

git revert commit_id (4글자정도)

